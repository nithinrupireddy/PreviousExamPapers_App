<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nithin.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.LoginActivity" android:parentActivityName=".Login.LoginActivity"/>
        <activity
            android:name=".Login.Signup" android:parentActivityName=".Login.LoginActivity" />
        <activity android:name=".Branches.Allbranches" />
        <activity
            android:name=".cse.Cseactivity" android:parentActivityName=".Branches.AllYears" />
        <activity android:name=".Branches.AllYears" android:parentActivityName=".Branches.AllYears"></activity>
        <activity
            android:name=".cse.Loadingactivity" android:parentActivityName=".cse.Cseactivity" />
    </application>

</manifest>